// SPDX-FileCopyrightText: 2025-2026 Evgenij Cjura and project contributors
// SPDX-License-Identifier: Apache-2.0
// Tier 2: Aqara QBKG11LM two-gang wall switch (neutral wire).
//
// Uses `factory::LumiOnOff` with a 2-endpoint label map. Emits
// `state_left` / `state_right` from `genOnOff` reports on endpoints
// 1 / 2 respectively.
//
// z2m-source: zigbee-herdsman-converters/src/devices/lumi.ts
//             #QBKG11LM (lumi.ctrl_ln2.aq1 / lumi.ctrl_ln2).

#include "definitions/lumi/_shared.hpp"
#include "definitions/lumi/factories.hpp"

namespace zhc::devices::lumi {

namespace {

struct QBKG11_Endpoints {
    static constexpr ::zhc::lumi::DeviceEndpointLabel entries[] = {
        {1, "state_left"},
        {2, "state_right"},
    };
    static constexpr ::zhc::lumi::DeviceEndpointLabels map{ entries, 2 };
};

using FX = ::zhc::lumi::factory::LumiOnOff<QBKG11_Endpoints>;

constexpr const char* kZigbeeModels[] = { "lumi.ctrl_ln1", "lumi.ctrl_ln1.aq1", "lumi.ctrl_ln2.aq1" };

}  // namespace


// --- auto-generated by emit_expose_bindings.py ---
constexpr Expose kAutoExposes[] = {
    {"battery", ExposeType::Numeric, Access::State, "%", nullptr, nullptr, 0},
    {"voltage", ExposeType::Numeric, Access::State, "mV", nullptr, nullptr, 0},
    {"device_temperature", ExposeType::Numeric, Access::State, "C", nullptr, nullptr, 0},
    {"power_outage_count", ExposeType::Numeric, Access::State, nullptr, nullptr, nullptr, 0},
    {"state", ExposeType::Binary, Access::StateSet, nullptr, nullptr, nullptr, 0},
};

constexpr BindingSpec kAutoBindings[] = {
    {1, 0x0000},
    {1, 0x0006},
};
// --- end auto-generated block ---

extern const PreparedDefinition kDefQBKG11LM{
    .zigbee_models       = kZigbeeModels,
    .zigbee_models_count=3,
    .model               = "QBKG11LM",
    .vendor              = "Xiaomi",
    .meta                = nullptr,
    .exposes=kAutoExposes,
    .exposes_count=sizeof(kAutoExposes)/sizeof(kAutoExposes[0]),
    .white_labels        = nullptr,
    .white_labels_count  = 0,
    .from_zigbee         = FX::fz_list,
    .from_zigbee_count   = FX::fz_count,
    .to_zigbee           = FX::tz_list,
    .to_zigbee_count     = FX::tz_count,
    .configure           = nullptr,
    .on_event            = nullptr,
.bindings=kAutoBindings,.bindings_count=sizeof(kAutoBindings)/sizeof(kAutoBindings[0]),
};

}  // namespace zhc::devices::lumi
