// SPDX-FileCopyrightText: 2025-2026 Evgenij Cjura and project contributors
// SPDX-License-Identifier: Apache-2.0
// Tier 2: Gs SGPHM-I1 — graduated from generated/ for a parity fix.
// Propane gas sensor
// z2m-source: gs.ts #SGPHM-I1 — m.iasZoneAlarm({zoneType:"gas", zoneAttributes:["alarm_2"]}).
//
// Parity fix (IAS dead-key + wrong bit): generic kFzIasZone emitted the
// bare key "alarm" off zoneStatus bit 0, but z2m's zoneType "gas" +
// alarm_2 publishes the semantic key "gas" off zoneStatus bit 1. Swapped
// to the typed kFzIasGasAlarm2 converter (emits "gas" from bit 1) and
// renamed the primary expose accordingly.
#include "definitions/_generic/_shared.hpp"

namespace zhc::devices::gs {
namespace {
const FzConverter* const kFz_SGPHM_I1[] = {
    &::zhc::generic::kFzIasGasAlarm2,
};

constexpr const char* kModels_SGPHM_I1[] = { "SGPHM-I1" };

}  // namespace


// --- auto-generated by emit_expose_bindings.py ---
constexpr Expose kAutoExposes[] = {
    {"gas", ExposeType::Binary, Access::State, nullptr, nullptr, nullptr, 0},
    {"tamper", ExposeType::Binary, Access::State, nullptr, nullptr, nullptr, 0},
    {"battery_low", ExposeType::Binary, Access::State, nullptr, nullptr, nullptr, 0},
};

constexpr BindingSpec kAutoBindings[] = {
    {1, 0x0500},
};
// --- end auto-generated block ---

extern const PreparedDefinition kDef_SGPHM_I1{
    .zigbee_models=kModels_SGPHM_I1, .zigbee_models_count=sizeof(kModels_SGPHM_I1)/sizeof(kModels_SGPHM_I1[0]),
    .manufacturer_name_prefix=nullptr,
    .manufacturer_names=nullptr, .manufacturer_names_count=0,
    .model="SGPHM-I1", .vendor="Gs",
    .meta=nullptr, .exposes=kAutoExposes, .exposes_count=sizeof(kAutoExposes)/sizeof(kAutoExposes[0]),
    .white_labels=nullptr, .white_labels_count=0,
    .from_zigbee=kFz_SGPHM_I1, .from_zigbee_count=sizeof(kFz_SGPHM_I1)/sizeof(kFz_SGPHM_I1[0]),
    .to_zigbee=nullptr, .to_zigbee_count=0,
    .configure=nullptr, .on_event=nullptr,
.bindings=kAutoBindings,.bindings_count=sizeof(kAutoBindings)/sizeof(kAutoBindings[0]),
};

}  // namespace zhc::devices::gs
