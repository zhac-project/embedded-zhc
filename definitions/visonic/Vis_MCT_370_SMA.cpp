// SPDX-FileCopyrightText: 2025-2026 Evgenij Cjura and project contributors
// SPDX-License-Identifier: Apache-2.0
// Tier 2: Visonic MCT-370 SMA — IAS dead-key fix.
// Magnetic door & window contact sensor
// z2m-source: visonic.ts #MCT-370 SMA (fz.ias_contact_alarm_1 -> zoneStatus
// bit0 = contact). The generated def lowered the generic kFzIasZone (bare
// key "alarm") while the expose declared the semantic key, so the contact
// state never reached the shadow. Swapped to the typed kFzIasContactAlarm
// (emits `contact` from bit 0) and renamed the expose.
#include "definitions/_generic/_shared.hpp"

namespace zhc::devices::visonic {
namespace {
const FzConverter* const kFz_MCT_370_SMA[] = {
    &::zhc::generic::kFzIasContactAlarm,
};

constexpr const char* kModels_MCT_370_SMA[] = { "MCT-370 SMA" };

}  // namespace


// --- auto-generated by emit_expose_bindings.py ---
constexpr Expose kAutoExposes[] = {
    {"contact", ExposeType::Binary, Access::State, nullptr, nullptr, nullptr, 0},
    {"tamper", ExposeType::Binary, Access::State, nullptr, nullptr, nullptr, 0},
    {"battery_low", ExposeType::Binary, Access::State, nullptr, nullptr, nullptr, 0},
};

constexpr BindingSpec kAutoBindings[] = {
    {1, 0x0500},
};
// --- end auto-generated block ---

extern const PreparedDefinition kDef_MCT_370_SMA{
    .zigbee_models=kModels_MCT_370_SMA, .zigbee_models_count=sizeof(kModels_MCT_370_SMA)/sizeof(kModels_MCT_370_SMA[0]),
    .manufacturer_name_prefix=nullptr,
    .manufacturer_names=nullptr, .manufacturer_names_count=0,
    .model="MCT-370 SMA", .vendor="Visonic",
    .meta=nullptr, .exposes=kAutoExposes, .exposes_count=sizeof(kAutoExposes)/sizeof(kAutoExposes[0]),
    .white_labels=nullptr, .white_labels_count=0,
    .from_zigbee=kFz_MCT_370_SMA, .from_zigbee_count=sizeof(kFz_MCT_370_SMA)/sizeof(kFz_MCT_370_SMA[0]),
    .to_zigbee=nullptr, .to_zigbee_count=0,
    .configure=nullptr, .on_event=nullptr,
.bindings=kAutoBindings,.bindings_count=sizeof(kAutoBindings)/sizeof(kAutoBindings[0]),
};

}  // namespace zhc::devices::visonic
