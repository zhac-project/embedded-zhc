// SPDX-FileCopyrightText: 2025-2026 Evgenij Cjura and project contributors
// SPDX-License-Identifier: Apache-2.0
// Tier 2: Itcommander ITCMDR_Contact — IAS dead-key fix.
// Contact Sensor
// z2m-source: ITCommander.ts #ITCMDR_Contact (fz.ias_contact_alarm_1).
//
// Gap (class c — IAS dead-key): the auto-port wired the generic
// `kFzIasZone`, which emits a bare `alarm` (zoneStatus bit 0) that
// never matched the `contact` expose, so the door/window state was
// dead. z2m's `fz.ias_contact_alarm_1` publishes the inverted bit-0
// as `contact` (closed = magnet present = bit0 clear = contact:true),
// plus `tamper` (bit 2) and `battery_low` (bit 3). The generic
// `kFzIasContactAlarm` (kLbl_Contact { "contact", bit 0, invert }) is
// the exact match — swap it in and re-point the exposes to the real
// semantic key (`contact`) instead of the dead `alarm`.
#include "definitions/_generic/_shared.hpp"

namespace zhc::devices::ITCommander {
namespace {
const FzConverter* const kFz_ITCMDR_Contact[] = {
    &::zhc::generic::kFzBattery,
    &::zhc::generic::kFzIasContactAlarm,
};

constexpr const char* kModels_ITCMDR_Contact[] = { "ITCMDR_Contact" };

}  // namespace


// --- auto-generated by emit_expose_bindings.py ---
constexpr Expose kAutoExposes[] = {
    {"contact", ExposeType::Binary, Access::State, nullptr, nullptr, nullptr, 0},
    {"battery", ExposeType::Numeric, Access::State, "%", nullptr, nullptr, 0},
    {"voltage", ExposeType::Numeric, Access::State, "mV", nullptr, nullptr, 0},
    {"tamper", ExposeType::Binary, Access::State, nullptr, nullptr, nullptr, 0},
    {"battery_low", ExposeType::Binary, Access::State, nullptr, nullptr, nullptr, 0},
};

constexpr BindingSpec kAutoBindings[] = {
    {1, 0x0001},
    {1, 0x0500},
};
// --- end auto-generated block ---

extern const PreparedDefinition kDef_ITCMDR_Contact{
    .zigbee_models=kModels_ITCMDR_Contact, .zigbee_models_count=sizeof(kModels_ITCMDR_Contact)/sizeof(kModels_ITCMDR_Contact[0]),
    .manufacturer_name_prefix=nullptr,
    .manufacturer_names=nullptr, .manufacturer_names_count=0,
    .model="ITCMDR_Contact", .vendor="Itcommander",
    .meta=nullptr, .exposes=kAutoExposes, .exposes_count=sizeof(kAutoExposes)/sizeof(kAutoExposes[0]),
    .white_labels=nullptr, .white_labels_count=0,
    .from_zigbee=kFz_ITCMDR_Contact, .from_zigbee_count=sizeof(kFz_ITCMDR_Contact)/sizeof(kFz_ITCMDR_Contact[0]),
    .to_zigbee=nullptr, .to_zigbee_count=0,
    .configure=nullptr, .on_event=nullptr,
.bindings=kAutoBindings,.bindings_count=sizeof(kAutoBindings)/sizeof(kAutoBindings[0]),
};

}  // namespace zhc::devices::ITCommander
