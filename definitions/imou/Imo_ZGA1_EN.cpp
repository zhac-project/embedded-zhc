// SPDX-FileCopyrightText: 2025-2026 Evgenij Cjura and project contributors
// SPDX-License-Identifier: Apache-2.0
// Tier 2: Imou ZGA1-EN — gas detector (IAS dead-key + channel fix).
// Smart gas detector
// z2m-source: imou.ts #ZGA1-EN — m.iasZoneAlarm({zoneType:"gas",
//   zoneAttributes:["alarm_1","alarm_2","tamper","test"], alarmTimeout:true}).
//
// Because BOTH alarm bits are requested, z2m's `bothAlarms` branch emits
// the split keys `gas_alarm_1` (bit 0) + `gas_alarm_2` (bit 1) — NOT the
// collapsed `gas` key. It also surfaces `tamper` (bit 2) + `test`
// (bit 8). The generated def lowered generic kFzIasZone (bare `alarm`/
// `alarm_1`) against a `alarm`+`tamper`+`battery_low` expose set: the gas
// state was dead, `test` was missing, and `battery_low` was a phantom
// (not in the z2m zoneAttributes). Swap in the imou-local
// kFzImouGasAlarm and align the exposes.
#include "definitions/_generic/_shared.hpp"
#include "definitions/imou/_shared.hpp"

namespace zhc::devices::imou {
namespace {
const FzConverter* const kFz_ZGA1_EN[] = {
    &kFzImouGasAlarm,
};

constexpr const char* kModels_ZGA1_EN[] = { "ZGA1-EN" };

}  // namespace


// --- auto-generated by emit_expose_bindings.py ---
constexpr Expose kAutoExposes[] = {
    {"gas_alarm_1", ExposeType::Binary, Access::State, nullptr, nullptr, nullptr, 0},
    {"gas_alarm_2", ExposeType::Binary, Access::State, nullptr, nullptr, nullptr, 0},
    {"tamper", ExposeType::Binary, Access::State, nullptr, nullptr, nullptr, 0},
    {"test", ExposeType::Binary, Access::State, nullptr, nullptr, nullptr, 0},
};

constexpr BindingSpec kAutoBindings[] = {
    {1, 0x0500},
};
// --- end auto-generated block ---

extern const PreparedDefinition kDef_ZGA1_EN{
    .zigbee_models=kModels_ZGA1_EN, .zigbee_models_count=sizeof(kModels_ZGA1_EN)/sizeof(kModels_ZGA1_EN[0]),
    .manufacturer_name_prefix=nullptr,
    .manufacturer_names=nullptr, .manufacturer_names_count=0,
    .model="ZGA1-EN", .vendor="Imou",
    .meta=nullptr, .exposes=kAutoExposes, .exposes_count=sizeof(kAutoExposes)/sizeof(kAutoExposes[0]),
    .white_labels=nullptr, .white_labels_count=0,
    .from_zigbee=kFz_ZGA1_EN, .from_zigbee_count=sizeof(kFz_ZGA1_EN)/sizeof(kFz_ZGA1_EN[0]),
    .to_zigbee=nullptr, .to_zigbee_count=0,
    .configure=nullptr, .on_event=nullptr,
.bindings=kAutoBindings,.bindings_count=sizeof(kAutoBindings)/sizeof(kAutoBindings[0]),
};

}  // namespace zhc::devices::imou
