// SPDX-FileCopyrightText: 2025-2026 Evgenij Cjura and project contributors
// SPDX-License-Identifier: Apache-2.0
// Tier 2: Multir MIR-SM100-E — smoke sensor (generic dual-alarm).
// z2m m.iasZoneAlarm({zoneType:"generic", zoneAttributes:["alarm_1",
// "alarm_2","tamper","battery_low"]}) — with zoneType "generic" + both
// alarms it publishes the RAW keys `alarm_1` (bit0) + `alarm_2` (bit1) +
// tamper + battery_low (NOT a `smoke` semantic key). The auto-port wired the
// generic kFzIasZone, which emits only a single `alarm` (bit0) and only on
// attribute reports — dropping `alarm_2` and the command-notification path.
// Swapped to kFzIasZoneStatusChange (decodes the cmd 0x00 notification → both
// alarm_1 + alarm_2 + tamper + battery_low). Exposes updated to alarm_1/2.
// (The SET-only `silence` enum has no decoder side and is not modelled.)
// z2m-source: multir.ts #MIR-SM100-E.
#include "definitions/_generic/_shared.hpp"

namespace zhc::devices::multir {
namespace {
const FzConverter* const kFz_MIR_SM100_E[] = {
    &::zhc::generic::kFzBattery,
    &::zhc::generic::kFzIasZoneStatusChange,
};

constexpr const char* kModels_MIR_SM100_E[] = { "MIR-SM100-E" };

}  // namespace


// --- auto-generated by emit_expose_bindings.py ---
constexpr Expose kAutoExposes[] = {
    {"battery", ExposeType::Numeric, Access::State, "%", nullptr, nullptr, 0},
    {"voltage", ExposeType::Numeric, Access::State, "mV", nullptr, nullptr, 0},
    {"alarm_1", ExposeType::Binary, Access::State, nullptr, nullptr, nullptr, 0},
    {"alarm_2", ExposeType::Binary, Access::State, nullptr, nullptr, nullptr, 0},
    {"tamper", ExposeType::Binary, Access::State, nullptr, nullptr, nullptr, 0},
    {"battery_low", ExposeType::Binary, Access::State, nullptr, nullptr, nullptr, 0},
};

constexpr BindingSpec kAutoBindings[] = {
    {1, 0x0001},
    {1, 0x0500},
};
// --- end auto-generated block ---

extern const PreparedDefinition kDef_MIR_SM100_E{
    .zigbee_models=kModels_MIR_SM100_E, .zigbee_models_count=sizeof(kModels_MIR_SM100_E)/sizeof(kModels_MIR_SM100_E[0]),
    .manufacturer_name_prefix=nullptr,
    .manufacturer_names=nullptr, .manufacturer_names_count=0,
    .model="MIR-SM100-E", .vendor="Multir",
    .meta=nullptr, .exposes=kAutoExposes, .exposes_count=sizeof(kAutoExposes)/sizeof(kAutoExposes[0]),
    .white_labels=nullptr, .white_labels_count=0,
    .from_zigbee=kFz_MIR_SM100_E, .from_zigbee_count=sizeof(kFz_MIR_SM100_E)/sizeof(kFz_MIR_SM100_E[0]),
    .to_zigbee=nullptr, .to_zigbee_count=0,
    .configure=nullptr, .on_event=nullptr,
.bindings=kAutoBindings,.bindings_count=sizeof(kAutoBindings)/sizeof(kAutoBindings[0]),
};

}  // namespace zhc::devices::multir
